## 8.4.0 LAN R16

**گروه‌ها**
- منوی جدید «گروه‌ها» در بخش «سازمان و پروژه». هر گروه بخشی از یک تیم است: یک سرگروه، اعضا (پشتیبان یا سرگروهِ همان تیم) و پروژه‌هایی که گروه مسئول آن‌هاست. هر نفر فقط در یک گروه است.
- اگر برای گروه پروژه تعیین شود، سرگروه و اعضا در فرم تسک، فیلترها و فهرست پروژه‌ها فقط همان پروژه‌ها را می‌بینند و سرور هم ثبت تسک یا ورود از اکسل روی پروژه دیگر را نمی‌پذیرد. تسکِ قدیمی که روی پروژه دیگری است هنگام ویرایش پروژه‌اش را نگه می‌دارد. گروه بدون پروژه همان محدوده تیم را دارد؛ مدیر، پلنر و نقش‌های سراسری محدود نمی‌شوند.
- زیرگروه‌های R14/R15 هنگام اولین اجرا خودکار به گروهی با نام «گروه + نام سرگروه» در تیم اصلی سرگروه تبدیل می‌شوند. فیلد «اعضای زیرگروه» از فرم کاربر برداشته شد و در فهرست کاربران نام گروه هر نفر دیده می‌شود.
- ساخت، ویرایش، تعیین اعضا، تعیین پروژه‌ها و بایگانی گروه هر کدام مجوز جدا دارد و فقط در تیمی مجاز است که فرد مدیر یا پلنر آن است. بایگانی تیم گروه‌هایش را هم بایگانی می‌کند.

**گزارش گروه‌ها**
- تب «گروه‌ها» در «گزارش‌های تیمی»: برای هر گروه تسک‌های انجام‌شده در بازه، باز، معوق، منتظر تأیید، برگشتی، درصد سروقت، سهم کار روی پروژه‌های خود گروه و ساعت کار؛ به تفکیک هر نفر و هر پروژه و با آخرین کارهای تمام‌شده. خروجی Excel و PDF به تفکیک نفر.
- سرگروه فقط گروه خودش را می‌بیند؛ مدیر، پلنر و کارشناس اجرایی همه گروه‌های محدوده خود را. صفحه گزارش‌ها روی اولین تبی باز می‌شود که نقش اجازه‌اش را دارد.
- فیلتر «گروه» به منوی «تسک‌ها» و «گزارش‌گیری» اضافه شد.

**مدیریت دسترسی**
- صفحه دسترسی‌ها بر اساس بخش‌های برنامه مرتب شد و هر بخش منوی خودش را کنار مشاهده و عملیاتش دارد. زیر هر بخش توضیح کوتاهی آمده و بخش مالی هشدار می‌دهد که عملیات مالی به تیم محدود نیست.
- مجوزهای تازه: شش مجوز گروه، «گزارش گروه‌ها»، «ثبت و حذف تعطیلات رسمی» (جدا از برنامه هفتگی)، «ثبت و حذف ماموریت همکاران» و «تأیید یا رد درخواست مرخصی» (جدا از ثبت مرخصی). در ارتقا هر نقش همان وضعیت مجوز قبلی را می‌گیرد؛ در نصب تازه تأیید مرخصی و ماموریت برای پلنر خاموش است.
- منوها فقط از روی صفحه دسترسی باز یا بسته می‌شوند. پیش از این بعضی منوها و فیلترها با فهرست ثابت نقش‌ها پنهان می‌ماندند؛ مثلاً داشبورد و فیلترهای تسک برای سرگروه.
- **رفع مشکل امنیتی:** ابزار SQL، پشتیبان‌گیری و دانلود فایل پشتیبان، خروجی کامل داده، نشست‌ها و اجرای خودکار در منو فقط برای مدیر سیستم بودند، ولی از راه API برای هر نقشی که مجوزشان روشن بود باز بودند؛ این مجوزها از R4 برای مدیر و پلنر پیش‌فرض روشن بود. حالا فقط مدیر سیستم به آن‌ها دسترسی دارد، در صفحه دسترسی قفل‌شده نمایش داده می‌شوند و هنگام اجرا از بقیه نقش‌ها برداشته می‌شوند.
- کنترل ثابت نقش از ۵۲ عملیات برداشته شد و همه فقط از صفحه دسترسی پیروی می‌کنند؛ مجوز این عملیات‌ها از قبل بررسی می‌شد و رفتارشان تغییری نمی‌کند. بایگانی تیم حالا محدوده تیم را بررسی می‌کند و ورود تسک از اکسل به تیم‌های واردکننده و پروژه‌های گروهش محدود است.

**بررسی**
- `sql/verify/VERIFY_R16_GROUPS.sql` فقط‌خواندنی است.

## 8.3.2 LAN R15

**سرگروه تسک‌های خود و زیرگروهش را مدیریت می‌کند**
- سه مجوز تازه کنار «ثبت تسک برای خود و اعضای زیرگروه»: ویرایش، حذف و «تأیید پایان و برگشت» تسک‌های خود و اعضای زیرگروه. همه برای سرگروه پیش‌فرض روشن‌اند و از «مدیریت دسترسی‌ها» قابل تغییرند؛ برای مدیر و پلنر خاموش‌اند چون مجوز کامل تیمی را دارند.
- سرگروه تسک‌های زیرگروهش را در کارتابل و منوی تسک‌ها می‌بیند. دکمه‌های ویرایش، حذف و تأیید/برگشت فقط روی همین تسک‌ها ظاهر می‌شوند و سرور دامنه هر عملیات را جداگانه بررسی می‌کند.
- ویرایش شامل عنوان، شرح، دسته، اولویت، تاریخ‌ها، پروژه و جابه‌جایی بین اعضای زیرگروه است. وضعیت فقط با شروع، ارسال و تأیید عوض می‌شود و وزن و اتصال قرارداد با پلنر می‌ماند.
- حذف فقط برای تسکِ تمام‌نشده است؛ تسک «پایان» سابقه و امتیازش را نگه می‌دارد.
- تسکی که سرگروه خودش تأیید کند برای خودش امتیاز ندارد (قانون تأیید توسط خود فرد).
- وقتی عضو زیرگروه کاری را برای تأیید می‌فرستد، سرگروهش هم اعلان می‌گیرد.

## 8.3.1 LAN R14

**نقش جدید: سرگروه**
- دسترسی‌ها دقیقاً مثل پشتیبان، به‌علاوه مجوز جدید «ثبت تسک برای خود و اعضای زیرگروه (سرگروه)».
- زیرگروه روی فرم کاربرِ سرگروه تعیین می‌شود (فقط پشتیبان‌ها و سرگروه‌های فعال) و جدا از تیم است.
- سرور تسک سرگروه را فقط برای خودش و اعضای زیرگروهش می‌پذیرد. تسک مستقیم ارجاع می‌شود و وزن، اتصال قرارداد و تأیید پایان کار با پلنر می‌ماند.
- سرگروه مثل پشتیبان کیف پول دارد و از تسک تأییدشده امتیاز و سکه می‌گیرد. در برنامه هفتگی، حضور، رتبه‌بندی و فهرست پشتیبان‌ها هم دیده می‌شود.

**پیامرسان کارفرما و راهبر**
- کارفرما و راهبر فقط با کاربران شرکت خودشان (کارفرما و راهبرهای همان پروژه) و پلنر تیمی که پروژه‌شان را انجام می‌دهد گفتگو می‌کنند. قانون در فهرست مخاطبان، ساخت گفتگو، تغییر اعضا و ارسال پیام روی سرور بررسی می‌شود.
- کارفرما و راهبر از گروه‌های خودکار تیم و اطلاعیه‌های سازمان خارج شدند. گفتگوی قدیمیِ خارج از این قانون خوانده می‌شود ولی پیام جدید نمی‌پذیرد.
- برای راهبر هم می‌شود پروژه تعیین کرد (اختیاری) تا شرکت او دقیق مشخص شود.

**نسخه موبایل**
- فهرست گفتگوها در موبایل تا آخر اسکرول می‌شود؛ پیش از این انتهای فهرست بریده می‌شد و بعضی مخاطبان در دسترس نبودند.

**بررسی**
- `sql/verify/VERIFY_R14_LEAD_CHAT.sql` فقط‌خواندنی است.

## 8.3.0 LAN R13

**امتیاز، سکه و فروشگاه**
- دو واحد جدید: امتیاز (سطح و رتبه، خرج نمی‌شود) و سکه (کیف پول، خرج می‌شود). پشتیبان از تسک تأییدشده امتیاز و سکه می‌گیرد؛ پلنر، کارشناس مالی و کارشناس اجرایی ماهانه با ستاره ادمین و کارنامه خودکار. مدیر و ادمین کیف پول ندارند.
- امتیاز تسک = ۱۰ × وزن پیشرفت × وزن دسته × کیفیت (برگشت) × سروقت (موعد لحظه واگذاری) × ستاره مدیر؛ تسک مشترک بر اساس زمان واقعی تقسیم می‌شود. تأیید توسط خود فرد امتیاز ندارد و بعد از ۱۵ تسک در روز امتیاز نصف می‌شود. حذف یا بازشدن تسک امتیاز را پس می‌گیرد.
- ارزیابی تسک به ۱ تا ۵ ستاره تبدیل شد؛ ارزیابی‌های قبلی در تاریخچه می‌مانند.
- سطح، فصل سه‌ماهه، ۱۱ نشان، مأموریت تیمی ماهانه، قدردانی «ممنونم» و رتبه‌بندی داخل تیم.
- صفحه «امتیاز و فروشگاه»: کارت من، فروشگاه با عکس و انواع آیتم (مرخصی تشویقی روزانه/ساعتی، دورکاری، پاداش مالی، کالا و کارت هدیه، تزئینی، صندوق تیم، سایر)، خریدهای من، تاریخچه عمومی، دفتر هدیه‌ها، تالار افتخارات.
- صفحه «مدیریت فروشگاه»: آیتم‌ها با عکس (ثبت، ویرایش، حذف/بایگانی، فعال‌سازی دستی)، جشنواره تخفیف درصدی جدا از فعال‌سازی، هدیه با دلیل مستند، لیست تحویل، لیست پرداخت، ستاره ماهانه، اقتصاد سکه و تنظیمات.
- خرید بدون تأیید و قفل‌شده روی کیف پول؛ مرخصی تشویقی خودکار در تقویم ثبت می‌شود و تا روز قبل قابل تغییر یا لغو با برگشت سکه است. سکه‌ها ۱۲ ماه بعد منقضی می‌شوند.
- فروشگاه در ابتدا بسته است؛ امتیاز و سکه از همان روز اول جمع می‌شود و ادمین از تنظیمات فروشگاه را باز می‌کند.

**راهنما**
- راهنمای نرم‌افزار به تب‌ها و بخش‌های جمع‌شونده تقسیم شد و جستجو در همه بخش‌ها اضافه شد. راهنمای امتیاز و فروشگاه برای هر نقش جداگانه نوشته شد.

**مدیریت دسترسی‌ها**
- گروه جدید «امتیاز، سکه و فروشگاه» با ۱۵ مجوز ریز و دو منوی جدید؛ پیش‌فرض هر نقش مطابق طرح تنظیم شد و همه از صفحه مدیریت دسترسی‌ها قابل تغییرند.

## 8.2.0 LAN R12

**فونت**
- فونت Vazirmatn (لایسنس SIL OFL) با هر دو وزن Regular و Bold داخل سورس قرار گرفت؛ Build دیگر به ZIP نسخه ۸.۰.۱ وابسته نیست.
- تعریف `@font-face` اصلاح شد؛ یک فایل استاتیک کل بازه وزن را ادعا می‌کرد و مرورگر برای متن‌های Bold وزن ضخیم نمی‌ساخت.
- خروجی PDF با فونت Vazirmatn ساخته می‌شود؛ پیش از این همه خروجی‌ها فقط `C:\Windows\Fonts` را می‌دیدند.

**پیام‌رسان**
- شمارنده پیام خوانده‌نشده در همه صفحات به‌روز می‌شود و اعلان (Toast، چشمک Tab، نوتیفیکیشن) اضافه شد.
- باز کردن گفتگو آخرین پیام‌ها را می‌آورد؛ پیش از این ۲۰۰ پیام اول خوانده می‌شد و پیام‌های اخیر قابل دیدن نبودند.
- اسکرول‌نشدن صفحه چت و بیرون‌رفتن کادر نوشتن از صفحه رفع شد.
- ظاهر پیام‌رسان بازطراحی شد: جداکننده روز، گروه‌بندی پیام‌ها، آواتار و ساعت داخل حباب.
- چیدمان پیام‌ها راست‌به‌چپ شد: پیام‌های خودتان سمت راست و پیام طرف مقابل سمت چپ با آواتار در لبه بیرونی؛ جهت هر پیام (فارسی یا فینگلیش) جداگانه تشخیص داده می‌شود.

**فهرست‌های کشویی**
- روی هم افتادن گزینه‌ها در فهرست‌های بلند رفع شد. در برخی مرورگرهای کلاینت ردیف‌ها فشرده می‌شدند و گزینه‌ها فقط پس از جستجو خوانا بودند. هر ردیف اکنون حداقل ارتفاع ثابت و خط جداکننده دارد و فهرست بلندتر است.

**دیتابیس**
- جدول‌ها لحظه اجرای EXE ساخته می‌شوند؛ پیش از این فقط وقتی کسی صفحه برنامه را باز می‌کرد ساخته می‌شدند و در حالت `--server` تا آن لحظه چیزی ساخته نمی‌شد.
- لاگ نام دقیق سرور، دیتابیس، Login و Schema و تعداد جدول‌ها را ثبت می‌کند. اتصال به دیتابیس سیستمی (مثل master، وقتی `TASKHUB_DB_NAME` خالی است) با پیام روشن رد می‌شود.
- اگر فایل `initial_admin_password.txt` نوشته نشود، خطا در لاگ ثبت می‌شود؛ پیش از این بی‌صدا رد می‌شد و رمز admin از دست می‌رفت.

**اهداف مالی**
- جهت تعریف هدف برعکس شد: هدف هر تیم را ثبت می‌کنید و هدف شرکت از جمع آن‌ها ساخته می‌شود.
- فیلد «هدف مصوب شرکت» برای مقایسه اضافه شد و دو منوی هدف در «اهداف مالی» ادغام شدند.
- گزارش تفصیلی هر تیم با بازه ماهانه، روند ماه‌به‌ماه و فهرست صورت‌وضعیت‌ها اضافه شد.

**دسترسی‌ها**
- کارشناس مالی مالکیت کامل اسناد مالی را گرفت؛ مدیر فقط اسناد تیم خودش را می‌بیند و نمی‌نویسد.
- پلنر دسترسی مالی ندارد و تأیید مرخصی دیگران با مدیر است.
- کارشناس اجرایی گزارش‌گیری سراسری و کاملاً فقط‌خواندنی شد.
- محدوده تیمی قرارداد و الحاقیه اصلاح شد؛ پیش از این برای همه نقش‌ها سراسری بود.
- دکمه «بایگانی‌ها» با امکان بازگردانی به سه صفحه مالی اضافه شد.
- قفل ویرایش صورت‌وضعیت پس از تأیید کارفرما به مجوز مستقل تبدیل شد.
- صفحه مدیریت دسترسی‌ها با جستجو، فیلتر، ردیابی تغییرات و ابزارهای گروهی بازطراحی شد.
- راهنمای داخل نرم‌افزار برای هر نقش بازنویسی شد.

## 8.1.1 LAN R11

- دفتر تلفن از مدیریت کاربران جدا و دارای مجوزهای مشاهده، مشاهده کل شرکت و ویرایش شماره شد.
- حساب شخصی دوباره با کلیک روی نام کاربر باز می‌شود و دکمه‌های آن مجوز مستقل دارند.
- شهرها برای همه تیم‌ها مشترک‌اند؛ پروژه‌ها همچنان تیمی هستند.
- خطای UNIQUE شهر تکراری با استفاده از رکورد موجود و پیام کاربرپسند رفع شد.
- قرارداد مرتبط تسک بر اساس پروژه، وضعیت باز و نوع تسک فیلتر و در حالت تک‌گزینه خودکار انتخاب می‌شود.
- گزینه‌های اطلاعات حساس پروژه به کارت پایین صفحه مدیریت دسترسی منتقل شدند.
- Migration و تست‌های انتشار R11 اضافه شد.

## 8.1.0 LAN R10

- همه کاربران غیرسراسری فقط داده‌های تیم خود را در کارتابل، پروژه، تسک، برنامه، گزارش و دفتر تلفن می‌بینند.
- محدوده کل شرکت فقط برای `admin`، `finance` و `reporter` فعال است.
- تیم اصلی در فرم ساخت/ویرایش کاربران تیمی اجباری است.
- RBAC تا سطح دکمه‌های شهر، پروژه، دسته‌بندی، تیم، فایل چت و اطلاعات حساس پروژه ریزدانه شد.
- پشتیبان با مجوز مشاهده/ویرایش، اطلاعات حساس پروژه‌های تیم خود را بدون وابستگی به تسک فعال مدیریت می‌کند.
- خطای بازکردن عکس و فایل چت رفع شد.
- نوع پروژه‌ها از `dbo.ProjectTypes` خوانده می‌شوند و داده‌های سفارشی حفظ می‌شوند.
- Migration و تست‌های انتشار R10 اضافه شد.

## 8.0.3 LAN R9

- بازه تاریخ گزارش تسک‌ها اکنون بر اساس تاریخ تأیید نهایی تسک محاسبه می‌شود، نه تاریخ ثبت تسک.
- عنوان ستون و خروجی‌های Excel/PDF به تاریخ تأیید تسک تغییر کرد.
- نسخه کش مرورگر و Service Worker به R8 ارتقا یافت.

## 8.0.3 LAN R7

- گردش ساده و قطعی تأیید/رد کارفرما.
- اصلاح وضعیت مالی صورت‌وضعیت‌های کانورت‌شده.
- مانده و پیشرفت ریالی فقط بر اساس صورت‌وضعیت تأییدشده.
- ویرایش قبل از تصمیم و قفل بعد از تصمیم.

## 8.0.3 LAN R6
- افزودن مجوزهای قابل واگذاری برای انتقال پروژه قرارداد دارای سابقه و ویرایش الحاقیه/صورت‌وضعیت در همه وضعیت‌ها.
- انتقال اتمیک وابستگی‌های عملیاتی و مالی قرارداد به پروژه و تیم مقصد همراه با ممیزی.
- فعال‌سازی ویرایش اسناد مالی و بازگرداندن سند ویرایش‌شده به پیش‌نویس.
- اصلاح فیلتر صفحات فرزند قرارداد و پاک‌سازی فیلتر هنگام ورود از منوی اصلی.
- اصلاح یک‌باره مبلغ ۴۷۵ صورت‌وضعیت کانورت‌شده و اصلاح کانورتر آینده؛ تشخیص Snapshot برای الحاقیه‌های 3029 و 3030.
- نمایش آخرین تاریخ پایان الحاقیه زمانی به‌عنوان تاریخ پایان مؤثر قرارداد.
- افزودن Sort گریدها و جداکننده سه‌رقمی در ورودی‌ها و نمایش مبلغ‌ها.
- تکمیل راهنما و آموزش داخل سامانه.

# TaskHub 8.0.3 LAN

- انتشار تک‌فایلی برای اجرای شبکه داخلی با IP و HTTP.
- Firewall خودکار روی سرور و Single Instance.
- Waitress چندنخی و اتصال SQL Thread-local برای جلوگیری از Timeout ناشی از صف Polling.
- Polling غیرهم‌پوشان در رابط کاربری.
- پیامرسان Server-managed با رمزگذاری داده در حالت سکون و بدون وابستگی به HTTPS/Web Crypto دستگاه‌ها.
- Backup و Restore کلید رمزگذاری پیامرسان.
- در R4 حالت LAN به‌صورت قطعی `server` است تا کلید چت به Login یا مرورگر وابسته نباشد.
## R3 — دسترسی و راهنما

- دسترسی سراسری مدیر و پلنر به پروژه‌ها، تسک‌ها، قراردادها، گزارش‌ها و کاربران بازگردانده شد.
- ثبت و ویرایش شهر، پروژه، قرارداد، اسناد مالی و کاربران عملیاتی برای هر دو نقش فعال شد.
- تایید نهایی، قفل مالی و بایگانی نهایی همچنان فقط برای مدیر و مدیر سیستم باقی ماند.
- حساب‌های admin/manager/planner در برابر تغییر توسط کاربران غیرادمین محافظت شدند.
- راهنمای داخل سامانه کوتاه، نقش‌محور و منطبق با نسخه LAN شد و توضیحات فنی منسوخ حذف شد.

## R4 — کنترل دسترسی و پایداری پیامرسان

- ماتریس دسترسی دیتابیس‌محور برای همه نقش‌ها و منوهای قابل واگذاری.
- کنترل هم‌زمان منو، دکمه و API؛ شامل مشاهده، ثبت/ویرایش/حذف، ارجاع، بررسی اولیه، تأیید، امتیازدهی، فایل، گزارش، خروجی، کاربران، قرارداد، تیم و پیامرسان.
- اتصال دکمه‌های افزودن تسک در داشبورد و صفحه تسک، و نیز ویرایش/حذف/ارجاع/تأیید به مجوزهای مستقل.
- هدایت کاربر به اولین صفحه مجاز و حذف وابستگی محدوده داده‌ها به نام ثابت نقش.
- همسان‌سازی پیش‌فرض مدیر و پلنر با مدیر سیستم، به‌جز مدیریت دسترسی‌ها.
- محافظت از حساب `admin` برای جلوگیری از دورزدن منوی مدیریت دسترسی.
- انتقال کلید پیامرسان LAN به `ApplicationSecrets` و واردکردن خودکار کلید نسخه قبلی؛ پایداری پیام‌ها در Login، مرورگر و Restart سرور.
- مقداردهی تنبل رمزنگار پیامرسان و جلوگیری از تعویض کلید در login/restart.

## 8.0.3 LAN R5
- افزودن فیلتر ماه شمسی به پایش هدف مالی و محاسبه هدف/عملکرد همان ماه.
- افزودن نمودار ماهانه و تجمعی به جزئیات برنامه مالی.
- تبدیل مقایسه پیشرفت تسک‌ها به سال/ماه شمسی و مقایسه مستقیم با ماه قبل.
- افزودن ویرایش نام گروه، حذف گروه، ترک گروه و حذف گفتگوی خصوصی با کنترل سمت سرور.
- تکمیل راهنمای سامانه برای قابلیت‌های جدید.
